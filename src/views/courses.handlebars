<!DOCTYPE html>
<html>
<head>
    <title>Lista de Cursos</title>
</head>
<body>
    <h1>Lista de Cursos</h1>
    <ul>
    {{#each courses}}
        <li>{{title}} - Profesor: {{teacherName}}</li>
    {{/each}}
</ul>
</body>
</html>
